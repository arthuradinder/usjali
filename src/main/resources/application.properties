spring.application.name=usajili

# Spring Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/registration
spring.datasource.username=root
spring.datasource.password=B1ko$mith0811
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging Level (Optional)
logging.level.org.springframework=INFO
logging.level.com.arthur.usajili=DEBUG

# Server Port (Optional, default is 8080)
server.port=8080

# Thymeleaf (if using for front-end views)
spring.thymeleaf.cache=false

# Security settings
spring.security.user.name=admin
spring.security.user.password=admin_password

# Other configurations as needed
# Custom properties
app.user.not-found-message=user with email %s not found
